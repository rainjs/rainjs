/**
 * The request handler
 * 
 * @param request the request object
 * @param response the response object
 */
function handler(request, response) {
    
}

module.exports = {
    name: "View Route",
    route: "",
    handler: handler
};