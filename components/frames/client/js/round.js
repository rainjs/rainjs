define([], function () {

    /**
     * This is the client-side controller for the 'round' page.
     *
     * @name RoundCorners
     * @class
     * @constructor
     */
    function RoundCorners() {}

    /**
     * Initialization lifecycle step that happens immediately after the controller is loaded.
     */
    RoundCorners.prototype.init = function () {
        // Add initialization logic here.
    };

    /**
     * Startup lifecycle step that happens right after the markup is in place.
     */
    RoundCorners.prototype.start = function () {
        // Add start logic here.
    };

    return RoundCorners;
});
